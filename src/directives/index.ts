export { fitText, useFitText } from './fitText'
export { autoFont } from './autoFont'

// 导出所有指令
import { fitText } from './fitText'
import { autoFont } from './autoFont'

export const directives = {
  fitText,
  autoFont,
}

export default directives
